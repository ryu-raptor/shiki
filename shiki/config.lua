---- shiki global setting
-- you can directly modify a table returned from this module
-- Lua's cache system persists your modifications

local config = {
    debug = false, -- debug mode (dump all parser's behaviour)

}

return config
